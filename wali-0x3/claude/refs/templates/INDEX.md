# 跨项目开发模板

本目录保存可跨项目复用的接口格式、错误语义、事件处理和代码模板。当前没有具体模板。

新增时登记：

| Ref ID | 路径 | 类型 | 适用场景 | 最近核对 |
| --- | --- | --- | --- | --- |

Developer 只在当前 Task 场景匹配时读取最小相关模板。模板不替代 Goal、项目代码事实或 Acceptance Method；项目需要偏离时，把选择与依据写入项目资料和 `goal.md`。
