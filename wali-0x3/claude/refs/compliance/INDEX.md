# 企业代码检查基线

本目录保存跨项目复用的代码质量、安全和工程检查资料。Developer 用于自检，Reviewer 用于独立审查。

当前没有具体基线。新增时登记：

| Ref ID | 路径 | 检查主题 | 读取者 | 最近核对 |
| --- | --- | --- | --- | --- |

项目特殊条款写入该项目 `docs/` 并编译进 `goal.md`；执行结果和发现写入 `work.md`。检查基线不能扩大 Scope、替代外部写入的当场确认或替代独立验证。
